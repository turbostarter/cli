---
"@turbostarter/cli": patch
---

Improve project setup by writing all configured Core provider environment values, reporting template JSON shape drift without stopping bootstrap, and preserving comments when updating Edge Wrangler JSONC.
